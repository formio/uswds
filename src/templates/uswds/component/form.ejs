<div
  id="{{ctx.id}}"
  class="{{ctx.self.hasInput ? 'usa-form-group ' : 'usa-prose '}} {% if (ctx.component.type === 'columns') { %}grid-row grid-gap{% } %}"
  {% if (ctx.styles) { %} styles="{{ctx.styles}}"{% } %}
  ref="component"
>
  {% if (ctx.visible) { %}
  {{ctx.children}}
  {% } %}
</div>
