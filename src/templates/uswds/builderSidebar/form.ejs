<div id="{{ctx.groupId}}" class="usa-accordion usa-accordion--bordered builder-sidebar{{ctx.scrollEnabled ? ' builder-sidebar_scroll' : ''}}" ref="sidebar">
  {% ctx.groups.forEach(function(group) { %}
    {{ group }}
  {% }) %}
</div>
