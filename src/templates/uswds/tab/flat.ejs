{% ctx.component.components.forEach(function(tab, index) { %}
  <div class="margin-bottom-2 usa-card">
    <div class="usa-card-header bg-default">
      <h4 class="mb-0 card-title">{{ ctx.t(tab.label) }}</h4>
    </div>
    <div class="card-body">
      {{ ctx.tabComponents[index] }}
    </div>
  </div>
{% }) %}
